apiVersion: v1
kind: ConfigMap
metadata:
  name: workflow-config
  namespace: cafeteria
data:
  DB_HOST: "workflow-db"
  DB_PORT: "5432"
  DB_DATABASE: "workflow_db"
  APP_PORT: "8082"
  USERS_SERVICE_URL: "http://users-service:8081"
  CORE_SERVICE_URL: "http://core-service:8080"
---
apiVersion: v1
kind: Secret
metadata:
  name: workflow-secret
  namespace: cafeteria
type: Opaque
stringData:
  DB_USERNAME: "dev"
  DB_PASSWORD: "root1234"
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: workflow-db
  namespace: cafeteria
spec:
  serviceName: workflow-db
  replicas: 1
  selector:
    matchLabels:
      app: workflow-db
  template:
    metadata:
      labels:
        app: workflow-db
    spec:
      containers:
        - name: postgres
          image: postgres:alpine
          env:
            - name: POSTGRES_DB
              value: "workflow_db"
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: workflow-secret
                  key: DB_USERNAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: workflow-secret
                  key: DB_PASSWORD
          ports:
            - containerPort: 5432
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "dev", "-d", "workflow_db"]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "dev", "-d", "workflow_db"]
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"
          volumeMounts:
            - name: workflow-db-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: workflow-db-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: workflow-db
  namespace: cafeteria
spec:
  selector:
    app: workflow-db
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-service
  namespace: cafeteria
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workflow-service
  template:
    metadata:
      labels:
        app: workflow-service
    spec:
      containers:
        - name: workflow-service
          image: cafeteria-workflow:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: workflow-config
            - secretRef:
                name: workflow-secret
          ports:
            - containerPort: 8082
          readinessProbe:
            httpGet:
              path: /health
              port: 8082
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8082
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: workflow-service
  namespace: cafeteria
spec:
  selector:
    app: workflow-service
  ports:
    - port: 8082
      targetPort: 8082